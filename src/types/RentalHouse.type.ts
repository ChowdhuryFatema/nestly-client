export type TRentalHouse = {
  location: string;
  rentAmount: string;
  bedrooms: string;
  description: string;
  images: string[];
};

export type TRentalRequest = {
  moveInDate: string;
  rentalDuration: string;
  message: string;
};
